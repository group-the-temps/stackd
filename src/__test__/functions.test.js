// const {_handleKeyDown} = require("../components/Header/Header")

// describe(`"_handleKeyDown" should not search based on certain inputs but alert the user`, () => {
//     test(`"_handleKeyDown" should alert the user of invalid input when { is first and } is last`, () => {
//         expect(_handleKeyDown()).toBe()
//     })
// })